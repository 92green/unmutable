{
  "private": true,
  "scripts": {
    "test-all": "lerna run test-all --concurrency 2",
    "build": "cp README.md packages/unmutable/README.md && lerna run build",
    "bootstrap": "lerna bootstrap"
  },
  "dependencies": {
    "lerna": "^3.10.7"
  }
}
